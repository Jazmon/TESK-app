import React from 'react';

export default class Login extends React.Component {
  render() {
    const {location} = this.props;

    return (
      <div>
        <h4>Login</h4>
      </div>
    );
  }
}
